# Database connection string
DATABASE_URL=postgresql://localhost:5432/aappr

# JWT secret for authentication (change in production!)
JWT_SECRET=placeholder_secret_change_in_production

# Server port
PORT=3000
